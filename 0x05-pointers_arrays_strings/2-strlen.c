#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the length of a string.
 * @s: Pointer parameter.
 * Return: void.
 */
int _strlen(char *s)
{
	int n;

	n = strlen(s);

	return (n);
}
