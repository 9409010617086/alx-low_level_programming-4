#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer.
 * @n: An integer parameter.
 * Return: An int.
 */
int _abs(int n)
{
	if (n > 0)
	{
		n = n;
	}
	else if (n == 0)
	{
		n = n;
	}
	else
	{
		n = -n;
	}
	return (n);
}
